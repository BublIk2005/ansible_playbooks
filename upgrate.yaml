- name: Ping Servers
  hosts: 
    - all
  become: true
  

  tasks:

  - name: Task ping
    ping:

  - name: Update cache
    apt:
      update_cache: yes

  - name: Upgrate
    apt:
      upgrade: yes